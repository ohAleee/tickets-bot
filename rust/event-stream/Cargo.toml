[package]
name = "event-stream"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
deadpool-redis = "0.11"
thiserror = "1.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
tokio = { version = "1", features = ["rt"] }
